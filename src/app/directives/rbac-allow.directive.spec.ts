import { RbacAllowDirective } from './rbac-allow.directive';

describe('RbacAllowDirective', () => {
  it('should create an instance', () => {
    const directive = new RbacAllowDirective();
    expect(directive).toBeTruthy();
  });
});
